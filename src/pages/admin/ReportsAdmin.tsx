/**
 * Admin reports editor re-ports in Phase 7 alongside monthly_reports. Keeps
 * its export so existing imports compile. Rendered nowhere right now, the
 * reports tab in ClientDetail was stubbed during Phase 4.
 */
export function ReportsAdmin() {
  return null;
}
