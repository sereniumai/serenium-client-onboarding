import { AppShell } from '../../components/AppShell';
import { ComingSoon } from './_ComingSoon';

export function FollowupSettingsPage() {
  return (
    <AppShell>
      <ComingSoon
        title="Follow-up emails"
        body="Follow-up nudge templates + cadence settings re-port here alongside Resend wiring (Phase 7 of the migration)."
      />
    </AppShell>
  );
}
