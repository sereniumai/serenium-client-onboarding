/**
 * Welcome video is re-ported on top of Supabase (welcome_video table +
 * welcomed_users dismissal state) in Phase 7. Renders nothing until then.
 */
export function WelcomeVideoModal() {
  return null;
}
