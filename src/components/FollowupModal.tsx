/**
 * Follow-up email modal re-ports in Phase 7 alongside Resend + the
 * followup_settings / followups_sent tables. Stubbed to keep the build green.
 */
interface Props { orgId: string; onClose: () => void }
export function FollowupModal({ onClose }: Props) {
  void onClose;
  return null;
}
