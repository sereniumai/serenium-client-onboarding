/**
 * AI chat is being re-ported on top of Supabase (ai_chat_messages,
 * analytics_uploads) in Phase 7 of the migration. Until then this component
 * renders nothing, the rest of the app keeps working.
 */
export function AiHelperChat() {
  return null;
}
