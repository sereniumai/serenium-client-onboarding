/**
 * ActivityFeed re-ports in Phase 7 when we port activity_log writes. Until
 * then this component is unused (ClientDetail's activity tab is gone) but the
 * export exists so any lingering imports compile.
 */
export function ActivityFeed() {
  return null;
}
